version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - npm install
  build:
    commands:
      - npm run prebuild
      - echo "hi"
  postbuild:
    commands: 
      - echo Build completed on `date`
artifacts:
  discard-paths: yes
  files:
    - src/*
  base-directory: dist/
