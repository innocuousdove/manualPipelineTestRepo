version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - npm install
  prebuild:
    commands:
      - npm run prebuild
      - echo "hi"
  build:
    commands:
      - npm run build
      - echo "hi"
  post_build:
    commands: 
      - echo Build completed on `date`
artifacts:
  discard-paths: yes
  files:
    - src/*

