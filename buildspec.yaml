version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - npm install
  build:
    commands:
      - npm run prebuild
      - npm run clean
      - npm run createDir
artifacts:
  files:
    - src/*
  base-directory: dist
